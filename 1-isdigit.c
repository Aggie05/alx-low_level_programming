#include "main.h"

/**
 * int _isdigit -  checks for a digit (0 through 9)
 * @c: the digit
 *
 * Return: Always 0.
 */
int _isdigit(int c);
{
	
	if (c >= 48 && c <= 57)
	{
		return (0);
	}
	
	return (0);
}
