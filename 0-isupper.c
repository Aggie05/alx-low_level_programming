#include "main.h"

/**
 * int _isupper - check if a number is upper
 * 0x: the number to be checked
 *
 * Return: 1 for upper letter or zero for any else
 */
int _isupper(int c);
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}

	return (0);
}
